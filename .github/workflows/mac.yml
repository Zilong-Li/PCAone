name: MacOS

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@v2

    - name: install dependency
      run: brew install gcc zstd

    - name: make
      run: |
        export CC=$(find $(brew --prefix)/bin/ -name "gcc-[0-9]*" | tail -1)
        export CXX=$(find $(brew --prefix)/bin/ -name "g++-[0-9]*" | tail -1)
        make

